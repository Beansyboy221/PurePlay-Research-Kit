name: PurePlay-Research-Kit
channels:
  - conda-forge
  - defaults
dependencies:
  # --- Base Dependencies ---
  - python=3.11     # Eventually update to >=3.14
  - pip

  # --- Conda Packages ---
  - pydantic
  - pywin32
  - pyarrow
  - pyarrow-stubs
  - pandas
  - polars
  - pytorch-gpu
  - lightning
  - lightning[extra]
  - tensorboard
  - optuna
  - optuna-integration
  - plotly
  - scikit-learn
  - sk2torch         # Replace by hummingbird-ml or nvtabular for >=3.14
  - blinker

  # --- Pip Packages ---
  - pip:
    - XInput-Python  #
    - keyboard       # Replace by inputs for >=3.14
    - mouse          #