import tkinter.simpledialog
import datetime

from ....utilities.prompt_utils.tkinter import tkinter_prompter

# @tkinter_prompter._build_gui.register(datetime.date)
# def prompt_for_date
# (data_type, root, title, message = 'Please enter a date/time (YYYY-MM-DD).'):
#     return tkinter.simpledialog.askstring(title, message, parent=root)
